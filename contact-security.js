(function(){
'use strict';
var SESSION_IDLE_MS=30*60*1000;
var ADMIN_FALLBACK='joaomonteiro2122@gmail.com';
function digits(v){return String(v||'').replace(/\D/g,'').slice(0,11)}
function phoneBR(v){var d=digits(v);if(!d)return '';if(d.length<=2)return '('+d;if(d.length<=6)return '('+d.slice(0,2)+') '+d.slice(2);if(d.length<=10)return '('+d.slice(0,2)+') '+d.slice(2,6)+'-'+d.slice(6);return '('+d.slice(0,2)+') '+d.slice(2,7)+'-'+d.slice(7)}
window.phoneBR=phoneBR;
function markPhoneFields(){['Proprietários','Inquilinos'].forEach(function(p){(forms[p]||[]).forEach(function(f){if(f[0]==='telefone'||f[0]==='telefone_emergencia')f[2]='tel'})});if(forms['Imóveis']&&!forms['Imóveis'].some(function(f){return f[0]==='inquilino_id'}))forms['Imóveis'].splice(1,0,['inquilino_id','Inquilino associado','lookup:inquilinos'])}
markPhoneFields();
var baseFormatCell=window.formatCell;window.formatCell=function(k,v){if(k==='telefone'||k==='telefone_emergencia'||k==='telefone_contato')return esc(phoneBR(v)||'—');if(k==='inquilino_id'){var inq=(cache.inquilinos||[]).find(function(x){return x.id===v});return inq?esc(inq.nome):'—'}return baseFormatCell(k,v)};
var baseColumnsFor=window.columnsFor;window.columnsFor=function(p){var cols=baseColumnsFor(p);if(p==='Imóveis'&&!cols.some(function(c){return c[0]==='inquilino_id'}))cols.splice(1,0,['inquilino_id','Inquilino']);return cols};
var baseModulePage=window.modulePage;window.modulePage=async function(){if(page==='Imóveis'&&!cache.inquilinos)await dbGet('inquilinos');return baseModulePage()};
function bindPhoneInputs(root){Array.prototype.forEach.call((root||document).querySelectorAll('input[type="tel"],#cfg_tel'),function(inp){inp.inputMode='tel';inp.maxLength=15;inp.placeholder='(68) 99999-9999';inp.value=phoneBR(inp.value);inp.addEventListener('input',function(){this.value=phoneBR(this.value)})})}
var baseOpenForm=window.openForm;window.openForm=async function(p,row){await baseOpenForm(p,row);bindPhoneInputs(document)};
setTimeout(function(){
  if(typeof window.config==='function'){var baseConfig=window.config;window.config=async function(){await baseConfig();bindPhoneInputs(document)}}
  if(typeof window.saveConfig==='function'){var baseSaveConfig=window.saveConfig;window.saveConfig=async function(){if(E('cfg_tel'))E('cfg_tel').value=phoneBR(E('cfg_tel').value);return baseSaveConfig()}}
  if(typeof window.accesses==='function'){var baseAccesses=window.accesses;window.accesses=async function(){await baseAccesses();if(currentRole!=='admin')return;try{var logs=await request('/rest/v1/login_auditoria?select=email,ip,user_agent,created_at&sucesso=eq.true&order=created_at.desc&limit=50',{headers:authHeaders(session.access_token)});var rows=(logs||[]).map(function(x){return '<tr><td>'+esc(x.email)+'</td><td>'+esc(new Date(x.created_at).toLocaleString('pt-BR'))+'</td><td>'+esc(x.ip||'—')+'</td><td class="muted">'+esc((x.user_agent||'—').slice(0,90))+'</td></tr>'}).join('');E('content').insertAdjacentHTML('beforeend','<div class="panel"><div class="panel-title"><h3>Últimos logins</h3></div><p class="muted">Registro de acessos bem-sucedidos ao sistema.</p><div class="tablewrap"><table class="table"><thead><tr><th>Usuário</th><th>Data e hora</th><th>IP</th><th>Dispositivo</th></tr></thead><tbody>'+(rows||'<tr><td colspan="4" class="empty">Nenhum login registrado.</td></tr>')+'</tbody></table></div></div>')}catch(_){}}}
},0);
function adminContactHtml(email){var e=email||ADMIN_FALLBACK;return '<div class="notice err"><b>Acesso temporariamente bloqueado.</b><br>Para recuperar o acesso, aguarde o prazo informado ou entre em contato com o administrador: <a href="mailto:'+esc(e)+'">'+esc(e)+'</a>.</div>'}
window.signin=async function(){var msg=E('msg'),email=E('email').value.trim(),pw=E('senha').value;if(!email||!pw){msg.innerHTML='<div class="notice err">Informe e-mail e senha.</div>';return}msg.innerHTML='<div class="notice">Validando acesso seguro...</div>';try{var res=await fetch(SUPA_URL+'/functions/v1/secure-login',{method:'POST',headers:{'apikey':KEY,'Content-Type':'application/json'},body:JSON.stringify({email:email,password:pw})});var data=await res.json().catch(function(){return {}});if(!res.ok){if(data.locked){msg.innerHTML=adminContactHtml(data.admin_email);return}var rem=typeof data.attempts_remaining==='number'?' Tentativas restantes: '+data.attempts_remaining+'.':'';msg.innerHTML='<div class="notice err">'+esc(data.error||'Não foi possível entrar.')+esc(rem)+'</div>';return}var s=data.session||{};s.user=data.user||s.user;saveSession(s);session=s;sessionStorage.setItem('matos_last_activity',String(Date.now()));await loadProfile();render()}catch(e){msg.innerHTML='<div class="notice err">'+esc(e.message||'Falha ao validar o acesso.')+'</div>'}};
function forceLogout(reason){if(!session)return;saveSession(null);session=null;currentRole='visitante';currentProfile=null;sessionStorage.removeItem('matos_last_activity');render();if(reason)setTimeout(function(){var m=E('msg');if(m)m.innerHTML='<div class="notice">'+esc(reason)+'</div>'},0)}
function touch(){if(session&&session.access_token)sessionStorage.setItem('matos_last_activity',String(Date.now()))}
['click','keydown','touchstart'].forEach(function(ev){document.addEventListener(ev,touch,{passive:true})});
setInterval(function(){if(!session||!session.access_token)return;var last=Number(sessionStorage.getItem('matos_last_activity')||Date.now());if(Date.now()-last>=SESSION_IDLE_MS)forceLogout('Sua sessão expirou após 30 minutos de inatividade. Entre novamente para continuar.')},30000);
var baseRestoreSession=window.restoreSession;window.restoreSession=async function(){var last=Number(sessionStorage.getItem('matos_last_activity')||0);if(last&&Date.now()-last>=SESSION_IDLE_MS){localStorage.removeItem('matos_session');sessionStorage.removeItem('matos_last_activity');return null}var s=await baseRestoreSession();if(s&&!last)sessionStorage.setItem('matos_last_activity',String(Date.now()));return s};
var baseShell=window.shell;window.shell=function(){baseShell();touch()};
})();